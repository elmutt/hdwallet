{
  "name": "@elmutt/hdwallet-keepkey-tcp",
  "version": "2.0.0-testnet",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "main": "dist/index.js",
  "source": "src/index.ts",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "prepublishOnly": "yarn clean && yarn build"
  },
  "dependencies": {
    "@elmutt/hdwallet-core": "^2.0.0-testnet",
    "@elmutt/hdwallet-keepkey": "^2.0.0-testnet",
    "axios": "^0.18.0",
    "bytebuffer": "^5.0.1",
    "eventemitter2": "^5.0.1"
  },
  "jest": {
    "modulePathIgnorePatterns": [
      "node_modules"
    ]
  }
}
