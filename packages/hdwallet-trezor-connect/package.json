{
  "name": "@elmutt/hdwallet-trezor-connect",
  "version": "1.10.1-testnet",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "main": "dist/index.js",
  "source": "src/index.ts",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "prepublishOnly": "yarn clean && yarn build"
  },
  "dependencies": {
    "@elmutt/hdwallet-core": "^1.10.1-testnet",
    "@elmutt/hdwallet-trezor": "^1.10.1-testnet",
    "bytebuffer": "^5.0.1",
    "eventemitter2": "^5.0.1",
    "trezor-connect": "^7.0.2"
  }
}
